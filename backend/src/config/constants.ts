/**
 * App-wide constants.
 */

/**
 * The single seeded user we treat as "logged in" (the assignment assumes a
 * default user). Must match the email created by prisma/seed.ts.
 */
export const DEFAULT_USER_EMAIL = 'demo@amazonclone.dev';
