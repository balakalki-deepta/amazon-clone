/**
 * Product Listing Page — the app's home route ("/").
 *
 * Placeholder for now; the grid, search results, and category filter are built
 * in the next step. Kept minimal so the shell is runnable on its own.
 */
export default function ProductListingPage() {
  return (
    <div style={{ maxWidth: 1500, margin: '0 auto', padding: '20px 16px' }}>
      <h1 style={{ fontSize: 21 }}>Products</h1>
      <p style={{ color: 'var(--text-muted)' }}>The product grid is coming in the next step.</p>
    </div>
  );
}
